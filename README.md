# 오목 돌 놓기 데모

간단한 오목 보드를 메모리 상에서 관리하며, 플레이어가 입력한 좌표에 돌을 두는 기능을 제공합니다. 이미 돌이 놓인 위치인지, 보드 범위를 벗어났는지를 검사하여 올바른 수만 허용합니다.

## 실행 방법

```bash
python gomoku.py
```

프로그램은 `B`와 `W` 플레이어가 교대로 수를 두도록 안내합니다. 좌표는 `x y` 형태의 두 정수를 입력하며, `q`/`quit`/`exit`를 입력하면 종료됩니다.
